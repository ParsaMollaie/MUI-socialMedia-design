import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#e1e1e1",
      light: "skyblue",
    },
    secondary: {
      main: "#999",
    },
    otherColor: {
      main: "#999",
    },
  },
});
